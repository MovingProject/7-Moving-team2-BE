### 로컬(.env.local)
PORT=3000
NEXT_URL=http://localhost:3000
EXTRA_ORIGINS=http://127.0.0.1:3000
# NEXT_PREVIEW_URL=   ← 비워둠

### 프리뷰(.env.preview)
PORT=3000
NEXT_PREVIEW_URL=https://moving-preview.vercel.app
# NEXT_URL=           ← 프리뷰 API라면 prod 도메인은 보통 허용하지 않음
# EXTRA_ORIGINS=

### 프로덕션(.env.production)
PORT=3000
NEXT_URL=https://moving.example.com
# NEXT_PREVIEW_URL=   ← prod에선 보통 프리뷰 허용X
# EXTRA_ORIGINS=